apiVersion: v1
kind: Secret
metadata:
  name: {{ include "contoso.fullname" . }}-postgresql
  labels:
    {{- include "contoso.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.global.postgresql.password | b64enc | quote }}
